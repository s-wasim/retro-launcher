# V7 — resume point

**Checkpointed:** 2026-09-01, after Task 6. **Branch:** `V7`, working tree clean.
**Head at checkpoint:** `73e25f4`. **Merge base with `main`:** `02e53bb`.

Plan: `docs/superpowers/plans/2026-09-01-v7-launcher-fixes.md`
Spec: `docs/superpowers/specs/2026-09-01-v7-launcher-fixes-design.md`
Ledger: `.superpowers/sdd/2026-09-01-v7-launcher-fixes/progress.md` — **git-ignored**,
which is why this file exists. If the ledger is gone, this file plus `git log` is the
recovery map.

## State

**Tasks 1-6 complete**, each with a task review returning spec ✅ and quality Approved.
**Tasks 7-12 not started.** Task 7's brief is generated but no implementer was
dispatched.

| # | Task | Commits | Review |
|---|---|---|---|
| 1 | Versioning + committed debug keystore + CI trigger | `80a829d..de5fb11` | clean |
| 2 | `PopupPlacement` + `AnchoredPopup` + drawer long-press | `33e7382..4262124` | clean |
| 3 | Dock slot REPLACE/REMOVE/MORE DETAILS popup | `8d19d87..e3dad7c` | clean |
| 4 | `HapticCurve` (core, 9 tests) | `f45fca6..9d5ed0b` | clean |
| 5 | `Haptics` engine, VIBRATE, FEEDBACK toggle, drag wiring | `6e24fcc..1d6a2ba` | clean |
| 6 | Haptics on 31 tap sites + 3 long-press sites | `d2fda44..6edab79` | clean, 1 fix round |

**Verified at checkpoint:** `:core:test assembleDebug` → BUILD SUCCESSFUL.
**212 tests, 0 failures**, across 20 core test classes.

## Resuming

Re-enter with `superpowers:subagent-driven-development`. The ledger's first line names
the plan, so the skill will resume correctly. Then:

1. **Task 7** — `ForegroundSpans` (core, pure TDD, 16 tests). BASE = current `HEAD`.
   Brief already generated at `.superpowers/sdd/…/task-7-brief.md`; regenerate with
   `scripts/task-brief <plan> 7` if the workspace was cleaned.
2. Tasks 8 → 12 in order. 8 and 9 must stay in order: Task 8 deletes
   `UsageMath.resolveTotal` and Task 9 removes its last caller, so the tree does not
   compile between them (`:core:test` still passes, since it does not build `app`).

Briefs are regenerated from the plan, so the plan file is the only thing that must
survive — and it is tracked.

## Carried forward

**One deferred minor**, for the final whole-branch review to triage:
`SettingsPanel`'s haptics listener is spelled `java.util.function.Consumer<Boolean>`
though the file already imports `Consumer` and uses the bare form in `toggleRow`. My
brief's inconsistency; cosmetic.

**Nothing is parked.** No task hit the fix-round cap; no finding was left unresolved.

**Task 12 does not push.** Ruling R5 removed `git push origin V7` from the plan —
pushing triggers the workflow, which publishes a public GitHub release with an APK.
That is the owner's call. When the work is done, the remaining command is:

```bash
git push origin V7
```

**One-time step for the owner, before the first V7 build will install:** the currently
installed launcher was signed with a runner-generated key that no longer exists.
Uninstall it once. Every build after that updates cleanly. Recorded in `BUILD.md`.

## Rulings made so far

Seventeen. R1-R6 came from the pre-flight scan; R7-R17 from checks run during
execution. Every one is recorded in full, with its cost-if-wrong, in the ledger.
Summarised:

| # | Ruling | Cost if wrong |
|---|---|---|
| R1 | Task 8's verification expectation contradicted itself; corrected to "exactly one hit" | none — a check, not behaviour |
| R2 | Task 9's grep matched `ScreenTimePanel` etc. so "no hits" was unachievable; replaced with two precise greps | none |
| R3 | Task 6 named three views that do not exist (`home.coffee`, `screenTime.limitSlider`, `drawer.setScrubberHaptics`); rewrote so parents forward | wrong wiring; caught at compile |
| R4 | Task 3 hedged on making constants public that already are | none |
| R5 | **Removed `git push origin V7` from Task 12** — publishes a public release | owner runs one command |
| R6 | Recorded that `ClockWidget` intentionally lacks `thud()` until Task 10 | none |
| R7 | Removed 171 MB of duplicate SDK dirs from a stale background job; pinned `build-tools/36.0.0` | none |
| R8 | **Task 3 tracked the touch point on `DockView`, where `ACTION_DOWN` never arrives** — every dock popup would have silently anchored wrong | would have shipped looking fine |
| R9 | My test expected `600`; correct is `630`. Implementer was right | none — verified by hand |
| R10 | `View.WINDOW_SERVICE` does not exist; `Context.WINDOW_SERVICE` | would not compile |
| R11 | Task 3 named two listener sites; only one is a `DockView.SlotActionListener` | compile failure |
| R12 | Enumerated all 31 haptic sites; found `LimitSlider` would double-tick | double buzz on one control |
| R13 | Matched `SettingsPanel`'s `rebuildXSection` convention and registered in `rebuildAll` | section would not re-colour on palette change |
| R14 | Task 10's copy grep returns 13 hits, 1 real; specified 3 exact edits + a leave-alone list | damage to unrelated code |
| R15 | Refreshed 12 stale line numbers after Tasks 3/5 shifted files; made quoted text authoritative | wrong lines edited |
| R16 | `AlphaScrubber`'s `ACTION_DOWN`/`ACTION_MOVE` share a fallthrough case; supplied the split | scrubber ignores a tap on the last-used letter |
| R17 | `UsageMathTest` already has a wildcard static import; de-qualified the added tests | redundant import |

## What changed on disk, tasks 1-6

New: `core/PopupPlacement.java` (+test), `core/HapticCurve.java` (+test),
`ui/AnchoredPopup.java`, `util/Haptics.java`, `app/debug.keystore`.
Modified: `app/build.gradle`, `.github/workflows/build.yml`, `BUILD.md`,
`AndroidManifest.xml`, `Prefs.java`, `HomeActivity.java`, `LauncherRoot.java`,
`DrawerPanel.java`, `DockView.java`, `SettingsPanel.java`, `BottomSheet.java`,
`SearchOverlay.java`, `ClockWidget.java`, `AlphaScrubber.java`, `LimitSlider.java`,
`CoffeeButton.java`, `ScreenTimePanel.java`.

## Toolchain

Local, working, verified:
- `JAVA_HOME=/opt/homebrew/opt/openjdk@17`
- `/opt/homebrew/opt/gradle@8/bin/gradle` — 8.14.5, matching the CI pin exactly
- SDK at `~/Library/Android/sdk`; `local.properties` points at it (git-ignored)
- `build-tools/36.0.0` — use the explicit path, not a glob: `35.0.0` is also installed

```bash
JAVA_HOME=/opt/homebrew/opt/openjdk@17 /opt/homebrew/opt/gradle@8/bin/gradle :core:test assembleDebug --no-daemon
```
