package com.retro.launcher.core;

/** The three shapes precipitation can take on screen — its own layer,
 *  independent of intensity or thunder. See DESIGN_NOTES §2b. */
public enum Precip { NONE, RAIN, SNOW }
