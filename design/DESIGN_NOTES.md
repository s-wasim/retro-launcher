# Design notes

Not yet populated — `design/prototype/` is empty. Once the Claude Design
prototype lands there, extract every token used (colors, background, text
sizes, row heights, padding/margins, font weight, alignment, spacing rhythm)
into the table below, alongside its native equivalent and any substitution
made (e.g. font-family fallback, an effect with no cheap native analog).

| Prototype token | Value | Native equivalent | Notes / substitutions |
|---|---|---|---|
| _(pending)_ | | | |
